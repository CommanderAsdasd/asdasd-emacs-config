;; minibuffer enable company completion
(add-hook 'minibuffer-setup-hook 'company-mode)
;; copilot completion
;; (add-hook 'minibuffer-setup-hook 'copilot-mode)

;; remove from hook
;; (remove-hook 'minibuffer-setup-hook 'company-mode)
;; (remove-hook 'minibuffer-setup-hook 'copilot-mode)
