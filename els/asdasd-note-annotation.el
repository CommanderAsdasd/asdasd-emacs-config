(use-package annotate
  :config (annotate-mode))

(use-package mpvi
  :ensure t
  :config
  ;; M-x customize-group mpvi
  ;; (setq mpvi-mpv-ontop-p t)
  ;; (setq mpvi-mpv-border-p t)
  (setq mpvi-cmds-on-init nil)

        )
  
